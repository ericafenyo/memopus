/**
 * A tag used to filter flashcards by category.
 */
export interface Tag {
  /**
   * Unique identifier for the tag.
   */
  id: string;

  /**
   * The name associated with the tag.
   */
  label: string;
}
